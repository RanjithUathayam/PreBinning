import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Material Movement Manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
